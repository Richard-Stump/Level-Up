package com.mygdx.nextlevel;

import com.badlogic.gdx.ApplicationAdapter;
import com.badlogic.gdx.graphics.Texture;
import com.badlogic.gdx.graphics.g2d.Sprite;
import com.badlogic.gdx.graphics.g2d.SpriteBatch;
import com.badlogic.gdx.utils.ScreenUtils;

public class Physics extends ApplicationAdapter {
    /*
    SpriteBatch batch;
    Texture player;
    Texture enemy;
    Sprite playerSprite;
    Sprite enemySprite;

    @Override
    public void create() {
        SpriteBatch batch = new SpriteBatch();
        player = new Texture("tyson.jpg");
        playerSprite = new Sprite(player);
        enemy = new Texture("enemy.jpg");
        enemySprite = new Sprite(enemy);
        batch.enableBlending();
    }

    @Override
    public void render() {
        ScreenUtils.clear(1, 0, 0, 1);
        batch.begin();
//        batch.draw(playerSprite, playerx, playery, 64, 64);
//        batch.draw(enemySprite, enemyx, enemyy, 64, 64);
        playerSprite.draw(batch);
        enemySprite.draw(batch);
        batch.end();
    }

    public void dispose() {
        batch.dispose();
        player.dispose();
        enemy.dispose();
    }
    */

}
